services:
  - type: web
    name: rnl-food-web
    runtime: node
    buildCommand: npm ci --only=production
    startCommand: npm start
    plan: starter
    region: frankfurt
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
